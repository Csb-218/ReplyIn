export interface cover {
    coverLetter: string
    generating: boolean
}

export interface JD {
    JD: string
    messageBox: HTMLElement
}

export type user = {
    email: string,
    family_name: string,
    given_name: string,
    id: number,
    name: string,
    picture: string,
    verified_email: boolean
}


